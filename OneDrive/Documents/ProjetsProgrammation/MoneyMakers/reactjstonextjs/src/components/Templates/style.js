import styled from "styled-components";

export const TemplatesTitle = styled.h1`
    font-size: 4rem;
    color: var(--color-dark-100);
    font-weight: 700;
    margin-bottom: -2rem;
    text-transform: capitalize;
`